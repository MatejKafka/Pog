All files in this directory are templates, the strings `{{NAME}}` and `{{VERSION}}`
are replaced with the respective values when creating a new manifest.

NOTE: Template placeholders must be wrapped in single quotes, not double quotes.